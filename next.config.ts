import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'kumrsrwcdqsojdvjifpo.supabase.co',
      // Add other domains if needed
    ],
  },
};

export default nextConfig;
